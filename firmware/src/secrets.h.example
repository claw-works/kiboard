// 复制为 secrets.h 并填入真实凭据
#pragma once

struct WifiCred {
    const char *ssid;
    const char *pass;
};

inline constexpr WifiCred WIFI_CREDS[] = {
    {"your-ssid", "your-password"},
};

// hub 地址
inline constexpr char HUB_HOST[] = "192.168.1.100";
inline constexpr uint16_t HUB_PORT = 8787;
inline constexpr char HUB_WS_PATH[] = "/device?token=change-me";
